# airflow-test-dags

Test dags for airflow deployment experiments to be used to test airflow deployment in k8s.
If you need to change the scripts, remember to update the Dockerfile or the requirements file.

For simplicity this example uses requirements.txt file and pip, which is not ideal in real world scenarios.